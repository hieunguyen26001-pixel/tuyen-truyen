# Một lần đến nhà, một lần gieo thêm niềm tin

Long-form bài viết tuyên truyền — static site.

## Chạy local

```bash
python3 -m http.server 8080
```

Mở: http://localhost:8080

## Deploy Vercel

```bash
npx vercel
```

Production:

```bash
npx vercel --prod
```
